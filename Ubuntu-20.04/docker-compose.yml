version: '3'
services:
  dev2:
    image: hossein20s/docker-workstation:dev
    container_name: dev2
#     command: tail -f /dev/null
    privileged: true
    ports:
      - 4333:4000
    environment:
      DISPLAY: # for mac DISPLAY=docker.for.mac.host.internal:0 # --before running docker-compose and after running xhost +
      USER: developer
      PASSWORD: developer
    cap_add:
      - SYS_PTRACE
    volumes:
      - ${HOME}:/opt/host
      - /dev/shm:/dev/shm
      # - ${HOME}/docker-workstation/developer:/home/developer # uncomment to persists user home
